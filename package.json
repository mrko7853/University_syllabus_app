{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sync:app-meta": "node scripts/sync-app-meta.mjs",
    "dev": "APP_MODE=development npm run sync:app-meta && vite",
    "build:aliases": "node scripts/create-route-aliases.mjs",
    "build:web": "APP_MODE=production npm run sync:app-meta && vite build && npm run build:aliases",
    "build": "npm run build:web",
    "build:prod": "VITE_APP_BUILD_LABEL=production npm run build:web",
    "build:staging": "VITE_APP_BUILD_LABEL=staging npm run build:web",
    "release": "node scripts/release.mjs"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@supabase/supabase-js": "^2.53.1",
    "bcryptjs": "^3.0.2",
    "build": "^0.1.4",
    "cors": "^2.8.5",
    "dotenv": "^17.2.1",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "nodemailer": "^7.0.5",
    "pg": "^8.16.3",
    "run": "^1.5.0",
    "vite": "^7.1.0",
    "wanakana": "^5.3.1"
  }
}
